machine:
  timezone:
    Australia/Sydney
dependencies:
  override:
    - ./scripts/ci_build.sh
test:
  override:
    - ./scripts/run_tests.sh

deployment:
  development:
    branch: master
    commands:
      - ./scripts/ci_deploy.sh

